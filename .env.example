# Server configuration
PORT=3000

# Firebase Admin credentials (optional: falls back to in-memory store if unset)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# Client Firebase configuration
FIREBASE_API_KEY=
